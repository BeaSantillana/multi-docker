sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t beasantillana/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true beasantillana/react-test npm test

after_success:
  - docker build -t beasantillana/multi-client ./client
  - docker build -t beasantillana/multi-nginx ./nginx
  - docker build -t beasantillana/multi-server ./server
  - docker build -t beasantillana/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push beasantillana/multi-client
  - docker push beasantillana/multi-nginx
  - docker push beasantillana/multi-server
  - docker push beasantillana/multi-worker
